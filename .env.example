# API Configuration
VITE_API_URL=http://localhost:5000

# Twilio Configuration (Optional - used for client-side configurations if needed)
# Most Twilio configuration is handled on the backend for security